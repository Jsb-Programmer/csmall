<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cskaoyan.mapper.UserMapper">

    <!--排序使用的是${}-->
    <select id="select" resultType="com.cskaoyan.bean.pojo.CskaoyanmallUser">
        select id,username,mobile,gender,birthday,last_login_time as lastLoginTime
        from cskaoyanmall_user
        <where>
          <if test="username != null">
              username = #{username}
          </if>
          <if test="mobile != null">
              and mobile = #{mobile}
          </if>
        </where>
        order by ${param.sort} ${param.order}
    </select>
</mapper>
